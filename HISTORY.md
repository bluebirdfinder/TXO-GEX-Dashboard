# 📜 TXO GEX Dashboard — 開發版本演進歷史全貌 (HISTORY)

> **「開發過程是至高無上的瑰寶」**  
> 本檔案完整記載「尋鳥 Bluebird Finder — TXO GEX 量化儀表板」自開發以來的所有重大版本突破、量化算式演進、籌碼研討與修復紀錄。本檔案與 [STATUS.md](STATUS.md) 保持實時同步。

---

## 📅 版本演進總覽時間軸

| 版本 | 發布日期 | 核心主題與重大突破 |
| :---: | :---: | :--- |
| **`v63.6`** | 2026-09-16 | 🔴🔴 **週選（週三/週五）結算日驗證出同一根因已影響歷史回補資料，回填修正 `session_snapshots.json` 09-09/09-11 共4筆快照（09-11錯誤數字原本正顯示在網站「5日歷程」表格上）發布版** |
| **`v63.5`** | 2026-09-16 | 🔴🔴 **月選結算日「已死合約」誤選重大修正（`classify_txo_contract_buckets()` 結算日誤選剛結算歸零的當月合約，Call Wall/Put Wall/Zero Gamma 全部算錯）發布版** |
| **`v63.4`** | 2026-09-16 | 🆕 **新功能：選擇權大額交易人淨部位（TAIFEX `largeTraderOptQry`）串接與 Call Wall / Put Wall 交叉印證徽章發布版** |
| **`v63.3`** | 2026-09-16 | 🔴🔴 **大額交易人前五大/前十大淨部位計算公式重大錯誤修正（欄位索引錯，算出來的從來不是淨部位）、含歷史快照回補發布版** |
| **`v63.2`** | 2026-09-16 | 🔴 **5日法人籌碼矩陣「+0」誤顯示修正為「—」（使用者實測親自抓到）、DAY session 法人快照補上 is_live 標記防止借用值被誤存成永久歷史（比照NIGHT session修法）發布版** |
| **`v63.1`** | 2026-09-16 | 🔴🔴 **散戶多空比分母算錯重大修正（跟券商數字完全對上）、真實FOMC利率決議與四巫日日曆、VIX歷史回補、法人籌碼歷史重複bug修復發布版** |
| **`v63.0`** | 2026-09-15 | 🚨 **首次真正部署上線！`main`分支這幾天從未merge過的重大發現、系統性假保底值清理三部曲（後端7支函式/前端CHART_DEFAULTS統一/總經雷達真數據重建）、room.js兩張從建立以來就是死的UI卡片修復發布版** |
| **`v62.4`** | 2026-09-15 | 🛠️ **尋鳥戰情室3個Critical bug修復（假OHLC面板/ADX背離寫死價位/選股雷達雜湊碼）、ADR真實報價、散戶籌碼日增減真數據、選股快取覆蓋率32%→97%發布版** |
| **`v62.3`** | 2026-09-14 | 🔴🔴 **GEX 核心引擎正式接上 TAIFEX 真實選擇權未沖銷部位（徹底移除假高斯曲線）、5日歷史真實回補、法人籌碼/選股雷達真數據發布版** |
| **`v62.2`** | 2026-09-12 | 🛡️ **Hard Redline #6 嚴禁偽數據實裝、戰情室 100% 真實數據管線、指數 0 成交量自檢校準、老墨/陳玠儒大戶散戶動能與 Gemini 2.5 Flash 多模態軍師發布版** |
| **`v62.1`** | 2026-09-11 | ⚡ **盤中即時台指期 (TXF) TAIFEX MIS Quote 串接升級、5日矩陣多盤別價差全自動精確校正** |
| **`v62.0`** | 2026-09-11 | 🌪️ **CBOE VVIX (波動率之波動率) 實時採集、4級尾部風險矩陣、賣腳安全氣墊 (350~500點外) 與戰情室 4 欄式 Macro Risk HUD 升級發布版** |

---

## 🎯 各版本詳細更新紀錄

### 🔴🔴 v63.6 週選結算日歷史回補資料同樣中招，回填修正發布版 (2026-09-16)
- **🔴🔴 使用者要求**：v63.5 只用真實資料驗證並修好「今天」這個monthly settlement day的即時計算路徑（`fetch_and_calc_vision.py` 主流程直接呼叫 `classify_txo_contract_buckets()`），使用者接著要求：「週選結算的你也幫我一起檢查和驗證」——因為同一支函式的 w1/w2/fri 桶邏輯完全共用同一段程式碼，理論上週選（每週三/週五結算）也該有一樣的風險，但v63.5沒有拿真實資料驗證過這件事。
- **🔴🔴 驗證方式**：直接呼叫 TAIFEX 官方 `optDataDown` 端點，抓 2026/08/26~2026/09/16 這段真實資料，涵蓋2026年9月**4個實際發生過的週選結算日**：09-02（週三）、09-04（週五）、09-09（週三）、09-11（週五）。逐一檢查每個結算日當天的報表，是否也跟月選結算日一樣，同時列出「剛結算的當週合約」與「下一口合約」：
  - 09-02：`202609W1`（到期=當天，已結算）call OI 89,835／put OI 61,353，同時列出 `202609W2`（到期09-09）。
  - 09-04：`202609F1`（到期=當天）call OI 65,201／put OI 61,393，同時列出 `202609F2`（到期09-11）、`202609F3`（到期09-18）。
  - 09-09：`202609W2`（到期=當天）call OI 66,557／put OI 69,396，同時列出 `202609W4`（到期09-23，跳過W3是因為第3週已經是monthly，不會另外發W3週選）。
  - 09-11：`202609F2`（到期=當天）call OI 70,763／put OI 52,990，同時列出 `202609F3`（到期09-18）。
  - **結論：4個結算日全部100%重現同一種資料源行為**——不是月選獨有的巧合，是TAIFEX `optDataDown` 報表在任何契約的結算日當天，都會把「結算前最終OI」的那筆資料留在報表裡，直到報表下次更新。
- **🔴 修正後的防呆邏輯逐一驗證通過**：分別模擬「結算日當天09:00（13:30結算前）」與「結算日當天15:52（13:30結算後，比照今晚夜盤快訊的實際時間）」兩個時間點呼叫修正後的 `classify_txo_contract_buckets(day_data, now=...)`：
  - 09:00（結算前）：正確選中即將結算的當週合約本身（此時它還沒結算，仍是真正在交易的前置週，選它是對的，不是bug）。
  - 15:52（結算後）：正確轉換到下一口——09-09從 `202609W2`→`202609W4`；09-11從 `202609F2`→`202609F3`。
  - 確認防呆邏輯的「時間感知」設計本身是正確的：不是「結算日當天就一律排除」，而是「結算日當天13:30後才排除」，跟真實市場交易時段完全吻合。
- **🔴🔴 回頭比對歷史資料，發現真正的問題不是程式碼邏輯本身（已驗證正確），而是「已經算錯並存進去的歷史快照」**：`data/session_snapshots.json` 現存的 09-09、09-11 兩天 DAY/NIGHT 共4筆快照，是v62.3上線「5日歷史真實回補」時，用 `backfill_snapshots.py` 呼叫**修正前**的 `classify_txo_contract_buckets()`（無時間檢查版本）算出來並永久寫入的。逐一重算比對：
  - 用舊版邏輯（無時間檢查，永遠選最早到期）重算，結果跟現存快照數字**逐位完全吻合**——直接證實這4筆快照當初就是用已結算歸零的當週合約算出來的：
    - `2026-09-09_DAY`：zero_gamma_level 47187.4（用已死的`202609W2`）；`2026-09-09_NIGHT`：47187.5（同上）。
    - `2026-09-11_DAY`：zero_gamma_level 45694.9（用已死的`202609F2`）；`2026-09-11_NIGHT`：45692.3（同上）。
  - 用修正後邏輯（13:30後排除當天到期合約）重算：
    - `2026-09-09_DAY`：zero_gamma_level **46556.0**（改用`202609W4`）、call_wall_strike 47500→**48000**；`2026-09-09_NIGHT`：zero_gamma_level **46556.4**、call_wall_strike 同上。
    - `2026-09-11_DAY`：zero_gamma_level **45554.2**（改用`202609F3`）、call_wall_strike 46500→**45700**；`2026-09-11_NIGHT`：zero_gamma_level **45242.5**、call_wall_strike 同上。
  - **這不是一個只存在歷史檔案裡、沒人看到的問題**：`fetch_and_calc_vision.py` 的 `history_10_sessions`／`history_6_sessions` 直接讀取 `session_snapshots.json` 組成「5日歷程」表格，而 09-11 剛好落在今天（09-16）往前數的 T-4~T-1 顯示區間內——換句話說，**修正前，網站上「5日歷程」表格裡 09-11 那兩列顯示的 Call Wall/Put Wall/Zero Gamma 數字，就是用已結算歸零的合約算出來的錯誤數字**，使用者當時看到的圖表是不準的。
- **🔴 修復方式**：不重新整段跑 `backfill_snapshots.py --overwrite`（會連帶重新對外抓取 spot/txf/otc/vix 等一大堆不相關且本身沒問題的欄位，徒增網路請求與出錯風險），而是只針對這4筆快照，直接用修正後邏輯重算 `zero_gamma_level`／`gex_plus_flip`／`call_wall_strike`／`put_wall_strike`／`max_pain_strike` 這5個受影響欄位並回填 `data/session_snapshots.json`，其餘欄位（spot_price/txf_price/pc_ratio/taifex_vix/us_vix/written_at）保持原值不動——因為這幾天的市場報價輸入本身沒有問題，問題只在合約桶選擇。回填後重跑一次 `fetch_and_calc_vision.py`，確認 `data/gex_data.json` 的 `history_10_sessions` 陣列裡 09-11 那兩筆已顯示修正後的正確數字。
- **📋 方法論補充**：這次驗證再次印證v63.5結尾提到的方法論——「換月結算日永遠是最容易出現資料源邊界案例的時間點」，這句話原來低估了風險範圍：不只是換月，**每一次週選結算（每週三、每週五）都同樣會遇到**，只是因為週選OI規模通常遠小於月選（例如09-11的`202609F2`前一天OI是70,763/52,990，量體不算誇張，不像月選結算那天93,035/87,447那麼顯眼），過去沒有被特別注意到、也沒被使用者的截圖比對抓到過。之後任何「结算日當天」的資料驗證，都應該同時檢查月選與週選兩條路徑，且要記得回頭稽核「已經產生並存進歷史檔案的舊資料」，不能只驗證「今天新算出來的資料」是否正確。

### 🔴🔴 v63.5 月選結算日「已死合約」誤選重大修正發布版 (2026-09-16)
- **🔴🔴 發現經過**：2026-09-16 是台指選擇權9月大結算日（每月第三個週三）。使用者收到16:00夜盤快訊後，懷疑其中「月選」相關的 GEX 點位（Call Wall/Put Wall/Zero Gamma/Max Pain）可能算錯，找到一個尚未證實的程式碼疑點：`classify_txo_contract_buckets()`（`scripts/fetch_and_calc_vision.py`）挑選「月選」合約的邏輯是把報表裡所有「6碼無週別後綴」的合約按真實到期日排序，直接取最早到期的那一口——完全沒有檢查「現在時間 vs 到期日」。上一個 session 因為 sandbox 網路白名單擋掉 taifex.com.tw（proxy 回403），無法連到期交所驗證這件事有沒有真的發生；本次 session 網路白名單已開通，得以完整驗證。
- **🔴🔴 根因確認**：直接呼叫 TAIFEX 官方 `optDataDown` 端點（`https://www.taifex.com.tw/cht/3/optDataDown?down_type=1&commodity_id=TXO`），抓 2026/09/10~2026/09/16 這段真實資料（cp950解碼），發現2026-09-16當天的報表裡，6碼月選合約同時列出：
  - `202609`（到期日=2026-09-16，即今天，已於13:30結算）：call OI 合計 93,035、put OI 合計 87,447——**結算前最終未平倉量，數字很大，不是空資料，容易被誤判為「有效」**。
  - `202610`（到期日=2026-10-21，真正的次月合約）：call OI 合計 8,801、put OI 合計 8,251。
  - 另有 `202611`／`202612`／`202703` 等更遠月份，OI 皆較小。
  - `classify_txo_contract_buckets()` 對6碼合約按到期日排序取最早一口，**選中已結算歸零的 `202609`**，而非真正該用的 `202610`。w1/w2/fri（`202609W4`/`W5`/`F3`）因到期日本身仍在未來，未受影響。
- **🔴 修復**：`classify_txo_contract_buckets()` 新增 `now` 參數（預設台北時區當下時間，`datetime.datetime.now(datetime.timezone(datetime.timedelta(hours=8)))`），排除「真實到期日 < 今天」或「到期日=今天且現在已過13:30結算時間」的候選合約，再從剩下的裡面挑最早到期。此防呆同步套用在 w1/w2/fri/mth 四種桶（同一根因，避免未來某週選剛好在結算日當天被誤留在報表裡重演）。呼叫端 `scripts/fetch_and_calc_vision.py` 主流程傳入既有的 `now_dt`（已是台北時區）。
  - **一併修正 `scripts/backfill_snapshots.py` 的呼叫端**：這支腳本是回補「過去某個歷史交易日」的快照，原本會不帶 `now` 呼叫（預設抓「執行當下的真實時間」），這樣會把所有過去交易日的候選合約全部誤判成「已過期」而清空——是這次加防呆差點引入的新 bug。已改用該歷史交易日當天的13:30（`ref_dt = datetime.datetime(day.year, day.month, day.day, 13, 30, tzinfo=tw_tz)`，原本就是後面才建立，這次提前到 `classify_txo_contract_buckets()` 呼叫之前）作為 `now` 基準，讓歷史回補的判斷跟「回補當天的市場視角」一致，而不是跟「腳本實際執行的今天」混淆。
- **🧪 驗證方式**：
  1. 用真實 TAIFEX 資料重跑修正後的 `classify_txo_contract_buckets(day_data, now=模擬15:52台北時間)`，確認 mth 桶正確選到 `202610`（到期2026-10-21，OI 8,801/8,251，非零）。
  2. 完整重跑 `fetch_and_calc_vision.py`，比對修正前後 `data/gex_data.json`（同一份即時價格輸入：spot 45848.9／txf 45529.0／day_txf 45759.0 完全一致，差異100%來自合約桶選擇，非市場報價漂移）：
     - `put_wall_strike`：45500 → **45000**（-500點）
     - `zero_gamma_level`／`gex_plus_flip`：45765.6 → **45158.5**（-607點）
     - `call_wall_strike`／`max_pain_strike`：46000／44950，不受影響
     - `total_gex_val`：643.22 → 962.49；`total_vex`：466.4 → 97.38
  3. 今晚（9/16）16:00夜盤快訊（`last_updated_time` 15:52）使用的舊版月選GEX數字已確認是用已死的9月合約算出來，不可信；已用修正後版本重新產生 `data/gex_data.json`／`encrypted_gex.json`／`embedded_data.js`。
- **📋 方法論補充**：換月結算日永遠是最容易出現「資料源仍列出剛死合約」這類邊界案例的時間點（週三月結算、週三/週五週選結算皆同理）。之後每逢結算日都該留意一個訊號：`classify_txo_contract_buckets()` 選中的桶，其到期日是不是恰好等於「今天」——如果是，且現在已過13:30，就該懷疑是不是選到了剛死的合約，而不是預設報表資料一定跟得上結算時效。

### 🆕 v63.4 選擇權大額交易人淨部位串接 ✕ Call/Put Wall 交叉印證徽章發布版 (2026-09-16)
- **🆕 新資料源：TAIFEX「選擇權大額交易人未沖銷部位結構表」(`largeTraderOptQry`)**：v63.3 修好的是期貨版大額交易人報表（`largeTraderFutQry`）；本專案過去從未抓過其選擇權姊妹報表，等於缺了「大額交易人在 Call/Put 分別站在多空哪一邊」這塊籌碼拼圖。新增 `fetch_official_taifex_large_trader_options()`，比照 v63.3 修好的「買方－賣方＝淨部位」公式（而非重犯 v63.3 之前那種同邊互減的錯），從一開始就用正確欄位索引解析。
  - **欄位結構已用真實 fetch 逐碼核對**（不是憑期貨版本欄位 index 直接套用——這正是 v63.3 出大 bug 的根因，這次刻意避免重蹈覆轍）：Call/Put 各自的「週契約」列在 col[0] 放商品名稱、col[1] 放「週契約」，數值從 col[2] 開始；「所有契約」列 col[0] 直接是列名，數值從 col[1] 開始。8 個數值欄位固定序：買方前五大(特定法人)、買方前五大%、買方前十大(特定法人)、買方前十大%、賣方前五大(特定法人)、賣方前五大%、賣方前十大(特定法人)、賣方前十大%。
  - 額外發現：這個端點的 HTTP 回應宣告且實際就是 UTF-8（跟 `largeTraderFutQry` 用 Big5 不同），一開始誤用 Big5 解碼導致中文全部變亂碼，改用 UTF-8 後正常。
  - 一併發現：對此端點發送不帶任何 POST 參數的空白 GET 請求，回傳的第一組資料就是台指選擇權（臺指買權/臺指賣權），跟期貨版本同一種「省略 contractId 直接拿到 TX 資料」的捷徑一致，不需要額外解析 `queryDate` 再組 POST。
  - 資料維度採「所有契約合計」為主要顯示數字（與 `fetch_official_taifex_large_trader()` 既有 `total` 欄位的口徑一致），週約另外以 `[週 ±X]` 金色徽章方式並陳，不做週約/月約/全月三選一的取捨。
  - 驗證：抓取結果 Call 前十大淨部位 -6,073、Put 前十大淨部位 +11,053，逐一比對 TAIFEX 官方頁面當下的原始 HTML 表格數字（買方前五大/十大、賣方前五大/十大、全市場未沖銷部位數），完全吻合。
- **📊 前端新增獨立表格「1.5 選擇權大額交易人淨部位 5 日歷程」**：緊接在既有「1. 期貨未平倉 5 日淨部位歷程」表格下方，欄位為 Call/Put 各自的前五大/前十大淨部位（週約/全約並陳）。併入既有 `institutional_5day_history` 陣列與 `_INST_DAY_FIELDS` 機制，完整比照本專案標準的 `has_snapshot`/`is_live` 防呆模式：沒有快照的歷史日期（含這個功能上線前就已存在的舊快照）一律顯示「—」，不會誤植 0 或借用今天的數字冒充過去。DAY session 的 `_day_is_live` 判斷條件新增這個資料源，任一來源這次抓取失敗，當天就不會被寫入永久歷史（跟既有 4 個法人籌碼來源的防呆邏輯一致）。
- **🐋 Call Wall / Put Wall 卡片新增大額交易人交叉印證徽章**：徽章邏輯——大額交易人在該側前十大淨部位為負（淨賣方，收權利金）視為「站在防守方」，用該牆體既有的強調色（Call Wall 紅／Put Wall 綠）顯示「防守中」；淨部位為正（淨買方，避險/加碼）視為有跌破/突破風險，用金色顯示「轉買方避險」警示。抓取失敗且無歷史可借用時，一律顯示「⚪ 無大額交易人即時數據」，絕不假設任何方向（比照 `AGENTS.md` 鐵律 6）。
- **🎨 呈現方式決策過程**：功能上線前，先用 mockup 視覺化工具畫出「獨立表格」與「卡片徽章」兩種呈現方案給使用者看過實際排版（而非只用文字描述），確認不會跟既有「三大法人選擇權淨金額（億 TWD）」表格（不同資料源：那張是金額、這張是口數）搞混後，使用者選擇「兩者都做」，先做獨立表格再做徽章。
- **🧪 驗證方式**：完整跑一次 `fetch_and_calc_vision.py` 產生真實 `gex_data.json`，本機 `python -m http.server` 開瀏覽器實測兩個新增畫面，數字與 TAIFEX 官網原始表格逐碼比對一致，且徽章顏色與文字邏輯符合預期（Put 前十大淨部位 +11,053 為正 → 正確顯示金色「轉買方避險」警示，而非誤判為防守）。

### 🔴🔴 v63.3 大額交易人前五大/前十大淨部位計算公式重大錯誤修正發布版 (2026-09-16)
- **🔴🔴 發現經過**：使用者親自截圖 TAIFEX 官方「大額交易人未沖銷部位結構表」（`largeTraderFutQry`）原始網頁，跟先前用 PyMuPDF 從台新期貨PDF「台指盤後籌碼」頁擷取的「台指期十大交易人 Futures OI」表格（十大合計／特定法人，近月/遠月/全月各一組數字）互相比對，發現跟我們`fetch_official_taifex_large_trader()`算出來的「前五大/前十大交易人淨部位」「特定法人淨部位」完全對不上——不是誤差，是數量級跟正負號都不同。
- **🔴🔴 根因**：官方表格每個月份分組（近月/全部月份）欄位結構是 `[月份標籤, 買方前五大(特定法人在括號內), 買方前五大%, 買方前十大(特定法人), 買方前十大%, 賣方前五大(特定法人), 賣方前五大%, 賣方前十大(特定法人), 賣方前十大%, 全市場未沖銷部位數]`（10欄，index 0~9）。正確的「淨部位」定義是同一排名層級的「買方－賣方」：前五大淨部位＝index[1]－index[5]，前十大淨部位＝index[3]－index[7]。
  - 原本的程式碼寫的是 `n_top5 = extract_val(near_r[1]) - extract_val(near_r[3])`（買方前五大－買方前十大）、`n_top10 = extract_val(near_r[5]) - extract_val(near_r[7])`（賣方前五大－賣方前十大）——這兩個都不是「買方－賣方」的淨部位計算，是同一邊（買方或賣方）內部兩個不同排名門檻互減，本質上是一個沒有實際交易意義的數字，從這支函式第一次被寫出來就是錯的。這個bug影響「5日矩陣」表格的「前五大交易人／前十大交易人／前五大特定法人／前十大特定法人」四欄，以及每個歷史日期旁邊的「[近 X]」近月金色徽章數字，跟`fetch_official_taifex_specific_traders()`重用這支函式產生的「外資特法背離診斷」文字。
- **🔴 修復與驗證**：
  1. 修正欄位索引為 `near_r[1]-near_r[5]`（前五大）與 `near_r[3]-near_r[7]`（前十大），near/total兩個月份分組都同步修正，far（遠月）維持用 total－near 反推（推導邏輯本身沒問題，不受影響）。
  2. 用修正後的公式重抓「今天」（此時仍是TAIFEX大額交易人報表T+1發布延遲下的9/15定案數字）：前十大合計淨部位 +3,506、特定法人合計淨部位 -785，近月分別是 -3,052／-7,900，遠月 6,558／7,115——跟台新PDF「十大合計」「特定法人」三組（近月/遠月/全月）共6個數字**逐位完全吻合**。
  3. **不只修「未來新抓的資料」，回頭修正被舊公式污染的歷史快照**：`data/institutional_snapshots.json` 裡 9/11、9/14 兩天的DAY session快照，當初就是用錯誤公式算出來並永久寫入的（例如9/11原本顯示top10_net=-23,839，是舊公式的產物）。改用TAIFEX官方頁面的「指定日期查詢」功能（`queryDate`參數，確認支援POST歷史日期查詢），重新抓取這兩天的官方原始表格，用修正後的公式重算並回填快照檔，讓「5日矩陣」表格裡過去幾天顯示的歷史數字也是正確的，而不是只有「今天」這一格是對的、過去幾天繼續錯下去。
  4. 瀏覽器實測：9/15列顯示「[近 -3,052] +3,506」「[近 -7,900] -785」，跟截圖完全一致；9/11、9/14兩列也改顯示回補後的正確數字。
- **📋 方法論補充**：這次PDF交叉比對能查到這個bug，關鍵是使用者主動去 TAIFEX 官網截圖原始表格（而不只是看程式碼「應該對」），還原了官方頁面的真實欄位標題與排列順序，才確認是欄位索引錯，不是資料源本身有問題。純文字擷取（即使用PyMuPDF正確抓到中文）仍然會漏掉表格的視覺分欄結構，這次是靠使用者的截圖補上這一塊——**之後遇到欄位順序有疑慮的表格類資料，最可靠的方式還是直接看一次官方頁面的視覺版面，不要只憑欄位index的既有假設**。

### 🔴 v63.2 5日法人籌碼矩陣「+0」誤顯示修正 ✕ DAY session is_live 標記發布版 (2026-09-16)
- **🔴 最高優先修復：「期貨未平倉5日淨部位歷程」與「現貨買賣超金額與選擇權金額5日歷程」兩張表格，沒有快照的日期誤顯示「+0」**：
  - 使用者實測親自抓到：`populateInstitutionalMatrix()`（`app.js`）對 `institutional_5day_history` 裡 `has_snapshot:false` 的日期（該天所有欄位都是後端明確寫入的 `null`，見 `fetch_and_calc_vision.py` 的 `_inst_null_day()`），用 `row.top5_net || 0` 這種寫法把 `null` 誤判成「+0」顯示，讓使用者誤以為「那天法人真的零進出」而不是「沒有真實資料」——跟9/15稽核時修過的 `night_institutional_trading` 表格是同一種bug，只是這兩張表當時漏修。
  - 已比照 NIGHT 表格的修法，新增 `cell()`／`cellAmt()` helper：`null` 一律顯示「—」（灰色），只有真實數字才顯示 `+`/`-` 號與顏色。兩張表共 10 個欄位（`top5_net`/`top10_net`/`top5_spec_net`/`top10_spec_net`/`foreign_fut_net`/`trust_fut_net`/`dealer_fut_net`/現貨三法人買賣超/Call-Put細分/`pc_ratio`）全部套用。
  - **一併發現並修正同類問題**：Table 2 的 `pc_ratio` 欄位，沒有快照的過去日期原本會 fallback 借用「今天」的即時 P/C Ratio (`row.pc_ratio || gexData.pc_ratio || CHART_DEFAULTS.pc_ratio`) 顯示在過去日期的欄位裡，等於把今天的數字誤植到過去某一天——已改為 `row.pc_ratio` 為 `null` 時直接顯示「—」，不再借用今天的數字冒充過去的資料。
  - 瀏覽器實測（本機 `python -m http.server`）：9/9、9/10（無快照）兩列全部欄位正確顯示「—」，9/11~9/15（有快照）維持正確顯示真實數字，全表文字掃描確認無殘留 `null`/`NaN`/`undefined`/多餘「+0」。
- **🟡 DAY session 法人快照補上 `is_live` 標記，防止借用值被誤存成永久歷史（風險比照9/15修過的NIGHT session bug）**：
  - 9/15 修過 NIGHT session 的「抓取失敗時借用上次真實值，卻被無條件寫進當天永久快照」bug（9/11與9/14數字因此一模一樣）。稽核確認 DAY session 的 `fetch_official_taifex_large_trader()`／`fetch_official_taifex_futures_institutional_oi()`／`fetch_official_taifex_options_matrix()`／`fetch_twse_institutional_stock_trading()` 這4支函式抓取失敗時一樣會靜默借用上次真實值，且原本 `t0_inst_day["has_snapshot"]` 是無條件寫死 `True`、`write_institutional_snapshot()` 也無條件執行——風險結構跟NIGHT完全相同，只是當時查證沒有找到實際已經重複的證據（可能只是運氣好）。
  - 已比照NIGHT的修法，4支函式的回傳值都新增 `is_live` 欄位（成功解析為 `True`，任何一步失敗落到last-real-value fallback為 `False`），並新增 `_day_is_live`（4個來源全部 `is_live` 才算 `True`）。`has_snapshot` 與是否呼叫 `write_institutional_snapshot()` 寫入永久歷史都改為看 `_day_is_live`，只有4個來源這次執行都是真正即時抓到才會寫入永久快照，避免借用值污染未來的歷史回溯。
  - 已完整跑一次 `fetch_and_calc_vision.py` 驗證：本次執行4個來源全部即時抓取成功，`_day_is_live=True`，`institutional_5day_history` 的T-0列正確顯示 `has_snapshot:true` 與真實數字，機制未破壞既有正常路徑。

### 🔴🔴 v63.1 散戶多空比分母算錯重大修正 ✕ 真實FOMC/四巫日日曆 ✕ VIX歷史回補 ✕ 法人籌碼歷史重複bug修復發布版 (2026-09-16)
- **🔴🔴 最重大修復：散戶多空比（小台/微台）分母計算錯誤，跟真實券商報告不符**：
  - 使用者用富邦App截圖、永豐(SinoPac)與台新(Taishin)兩家券商的真實盤後報告/PDF交叉比對，發現我們算出來的微台散戶多空比 31.27% 跟兩家券商公布的 40.95% 對不起來，小台也有類似落差。
  - 根因查到 `parse_taifex_fut_oi()`：(1) 近月未沖銷契約數（TAIFEX `futDailyMarketReport` 表格第12欄）被莫名其妙多乘了一個2——這欄本身就是完整的未沖銷契約數，不需要加倍；(2) 「合計」列的抓取邏輯是「取該列所有欄位裡最大的數字」，結果抓到的其實是成交量（一個大很多、不相關的數字），不是真正該欄位（同樣是第12欄）的未沖銷契約數合計。這兩個bug疊加，讓分母被灌水，算出來的比率被稀釋、失真。
  - 修正後：小台 -53口÷全月合計38,910口=-0.14%、微台 46,684口÷全月合計113,994口=40.95%——**跟永豐、台新兩家券商的數字完全一致（精確到小數點）**。同時把「主要顯示比率」的基準從「近月單一契約月」改為「全月合計未沖銷契約數」，因為這才是真實市場慣例（近月版本改列為次要參考數字）。
  - 額外發現：`futDailyMarketReport` 這個端點的中文標籤（「合計」「小計」）用 `big5` 或 `cp950` 解碼都會亂碼（純數字內容不受影響），改用「該列前兩欄是否為空字串」這種不依賴文字內容的結構特徵來判斷合計列。
- **🌐 總經事件雷達新增真實 FOMC 利率決議 + 真實美股四巫日**：
  - FOMC：直接抓聯準會官網 `federalreserve.gov/monetarypolicy/fomccalendars.htm`——這個頁面外觀像 Angular 動態應用，但會議日期清單本身其實是伺服器端直接輸出的純HTML（`div.fomc-meeting__month`/`div.fomc-meeting__date`），不需要另外找JSON API。聯準會官方會預先公布約1~2年的行事曆，所以這個日曆會隨每次執行自動保持最新，不會像寫死清單一樣過期。
  - 四巫日（Quadruple Witching Day）：3、6、9、12月第三個星期五，純日期算法（跟既有「每月第三個星期三」月結算邏輯同一套手法），完全不需要外部資料源，永遠不會過期。
  - 已用使用者提供的期貨商截圖交叉比對：FOMC顯示 9/17 02:00（台灣時間）、四巫日顯示 9/18，跟截圖完全吻合。
- **📊 歷史VIX回補（`backfill_snapshots.py`）**：5日GEX結構歷程矩陣裡，9/2~9/11這幾天的台指VIX/美股VIX過去顯示「—」（因為即時快照系統是最近才開始記錄這幾個欄位），現在改用 TAIFEX `getVixData` 端點（發現這個端點本來就支援指定任意歷史日期查詢，不是只能查當天）與 Yahoo Finance 歷史區間查詢，把這幾天的VIX真實補齊。**櫃買指數(OTC)回補失敗**：Yahoo Finance `^TWOII` 的歷史K棒API目前不管怎麼查都回傳空值（即時報價正常，只有歷史序列壞掉），懷疑是Yahoo那邊的資料問題，列為後續待查項目。
- **🔧 法人籌碼歷史重複bug修復**：發現「近5日夜盤三大法人交易籌碼歷程矩陣」裡9/11跟9/14兩天數字一模一樣。根因是先前（v63.0）加的「抓取失敗就退回上次真實值」邏輯，被無條件寫進當天的永久歷史快照——9/14當天即時抓取若失敗，借用的9/11舊數字就被誤存成「9/14的真實快照」，兩天數字因此永久重複、無法回溯更正。已加上 `is_live` 標記，只有真正抓到即時資料才允許寫入永久歷史，抓取失敗時誠實顯示「無快照」而非借用舊數字冒充。
- **📋 流程教訓（本身也值得記錄）**：上述4項修復完成當下，因為session用量即將到達上限，只用一般commit訊息記錄就直接merge上線，**沒有依照本專案「發版SOP」跑版本升級與補寫本檔案**，導致v63.0這個版號實際部署的內容比本檔案v63.0條目描述的還要多一批東西、對不起來。使用者發現後直接指出「你不是把這個SOP寫進skill了嗎？」——經討論後**使用者明確要求：以後遇到會影響交易判斷的重大修復，完成後應該由AI自己主動觸發發版流程，不用等使用者開口說「發版」**，已同步更新 `.claude/skills/release/SKILL.md` 觸發條件反映這個決定。

### 🚨 v63.0 首次真正部署上線 ✕ 系統性假保底值清理三部曲 ✕ room.js死卡片修復發布版 (2026-09-15)
- **🚨🚨 本次最重大發現：過去3天（v62.2~v62.4）所有修復，從未真正部署到 `bluebirdfinder.github.io` 網站過**：
  - 稽核發現：這3天所有工作（含v62.3的GEX核心引擎真實選擇權接軌、v62.4的3個Critical bug修復）全部只推到 `claude/bold-galileo-dy1oxb` 這條 feature branch，**從未 merge 進 GitHub Pages 實際讀取的 `main` 分支**。使用者一直看到的線上版本停留在 `main` 上 2026-09-14 凌晨的舊commit（比v62.3還早），版號顯示v62.2，且5日GEX結構歷程矩陣等修復完全沒有反映到線上——不是沒修好，是修好的東西從來沒有上線過。
  - 根因之二：`.github/workflows/auto_update.yml` 這支定時排程（每天固定時段重跑 `fetch_and_calc_vision.py` 更新盤後資料）是照 GitHub 預設分支（`main`）上的**舊版腳本**在跑，跟這幾天在feature branch上修復的邏輯完全是兩套——網站這幾天「看起來有在動」的假象，其實是舊版有假資料bug的腳本在自動跑。
  - 已將 `claude/bold-galileo-dy1oxb`（25筆commit）merge進 `main` 並push，`data/gex_data.json`/`embedded_data.js`/`encrypted_gex.json` 三個檔案因為排程機器人這幾天也在main上獨立改動而衝突，已解決衝突後用修好的pipeline重新產出最新真實數據。**這是這幾天所有self-audit修復第一次真正讓真實使用者看到**。
- **🔴🔴 最嚴重新發現並修復：核心特定法人分歧分析函式抓了網頁卻從未解析**：
  - `fetch_official_taifex_specific_traders()` 抓了 `largeTraderFutQry` 的HTML，但**從未解析過**，直接用寫死的5個數字產生「外資特法背離診斷」文字，每次執行都一樣。已改為重用 `fetch_official_taifex_large_trader()` 已驗證的真實解析結果，不必重新開發爬蟲；缺資料時明確標記 `UNAVAILABLE`，不再靜默套用舊字面值。
  - 個股期貨列表「🚀投信波段認養」徽章由 `idx` 取模公式湊出來，跟投信今天買不買毫無關係，已誠實停用（比照 room.js 選股雷達同款問題的處理方式）。
- **🛡️ 系統性靜默假保底值清理（7支後端函式 + app.js 34+處不一致常數）**：
  - `fetch_official_taifex_tx_prices()`（現貨價本身，全引擎輸入源頭）、`fetch_official_taifex_vix()`（VIX/VVIX）、`fetch_twse_institutional_stock_trading()`、`fetch_official_taifex_large_trader()`、`fetch_official_taifex_futures_institutional_oi()`、`fetch_official_taifex_options_matrix()`、`fetch_taifex_night_institutional_trading()` 全部改為「抓取失敗就退回上一次真實成功抓到的數值」，取代寫死screenshot數字永遠不變的舊模式。
  - `parse_taifex_fut_oi()` 內「魔術數字比對」鏈式假保底（比對 `36258`/`80167` 判斷抓取是否失敗，失敗就換另一組寫死多空拆分）已改為每個欄位獨立退回真實上一筆快照值。
  - `app.js` 新增 `CHART_DEFAULTS` 常數，統一34+處原本互相矛盾的保底字面值（例如 `zero_gamma_level` 原本在6個函式裡有6種不同數字）。
- **💰 融資維持率查證與重新設計**：實地查證確認 TWSE 從未公布過全市場整戶維持率官方數據（帳戶層級概念，官方不彙總揭露），過去的估算公式（`158.4 + 變動量×0.03`）改為用真實大盤漲跌幅與真實融資餘額變動動態校正，並在資料與畫面上明確標註 `is_estimated: true` / 「估算值」，不再假裝是官方數據。
- **🌐 總經事件雷達真數據重建**：`macro_risk_dashboard`（DXY/US10Y/VIX卡片）原本完全零fetch、打字寫死，且跟同引擎別處抓到的真VIX不同步，已改為DXY/US10Y抓真實Yahoo Finance報價，VIX重用真實值。`macro_events_calendar` 原本是會過期的寫死5筆事件清單（09/16後悄悄變成永久空清單，無任何提示），已改為重用同函式裡本來就是真實日期運算、永不枯竭的 `valid_events`（週/月結算、NFP、CPI、ADP、失業金、富台結算、MSCI調整）。
- **🔴🔴 尋鳥戰情室發現2張從功能建立以來就是死的UI卡片，已修復**：
  - 「GEX造市商五大防線」卡片（Call Wall/VEX轉折/Zero Gamma/Put Wall/Max Pain）：程式碼已經算出真實數值並正確用在旁邊的「距現價」欄位，但從未寫回卡片本身的顯示欄位，永遠顯示room.html裡打字寫死的初始值。
  - 「法人籌碼體質」卡片（外資期貨淨留倉/大盤P-C Ratio/融資維持率）：連 `id` 屬性都沒有，純靜態HTML文字，room.js從頭到尾沒有引用過。
  - 兩者已接上真實數據並實測確認正確；同時發現並統一了 room.js 內另外6處對GEX關卡欄位各自寫死不同保底值的問題（新增 `ROOM_CHART_DEFAULTS`）。
  - **附帶教訓**：`room.js` 的 `<script src="room.js?v=...">` 版本查詢字串長期沒更新，導致瀏覽器無限期快取舊版腳本，測試時容易誤判修復無效——已更新版本字串，並記錄「以後改room.js一定要同步bump版本字串」。
- **🧪 方法論升級：首次針對這兩個前端檔案做真實瀏覽器測試**（新增 `.claude/launch.json` 本機伺服器設定），而非只憑閱讀程式碼判斷「應該沒問題」——本次修復的2個app.js顯示bug（null值誤判、雙重正負號）與2個room.js死卡片，全部是唯有實際打開瀏覽器才會發現，純讀程式碼看不出來。

### 🛠️ v62.4 尋鳥戰情室3個Critical bug修復 ✕ ADR真實報價 ✕ 散戶籌碼日增減真數據 ✕ 選股快取97%覆蓋率發布版 (2026-09-15)
- **🔴 尋鳥戰情室 3 個 Critical bug 修復**：
  - 假OHLC面板：`renderLeftPanel()` 切換TXF/TAIEX/OTC時漲跌改用真實 `gexData` 換算，昨收改用真實現價減真實漲跌反推；開盤/最高/最低目前無真實逐筆盤中來源，誠實顯示「—」取代原本永遠不變的寫死文字。
  - ADX Pro V3背離寫死絕對價位：改成比較「本次波段極值」與「上次波段極值」當下的真實ADX值判斷背離，不再綁定任何固定價位，價格永久脫離舊區間後依然成立。
  - 選股雷達雜湊碼：改接真實 `screener_cache.json`，沒有真實訊號的股票直接跳過不顯示，不再用股票代號雜湊值捏造訊號。
- **🔧 ADR連動修復**：`ADR_MAPPING` 原本用到外層迴圈殘留變數導致每列對錯股票，已修正為用當列自己的股票代號；ADR漲跌幅改為即時抓取 Yahoo Finance 真實報價，不再是寫死常數。
- **📊 散戶籌碼日增減真數據**：`fetch_official_taifex_retail_sentiment()` 的 `daily_change`／`prev_ratio`／`broker_snapshot`（外資台指期/選擇權Call/Put淨未平倉）全部改用真實數據與真實日對日快照比對，沒有前一日快照時誠實顯示「—」而非寫死數字。
- **📈 選股快取覆蓋率 32% → 97%**：發現逐檔查詢（1400+次請求）本身就是觸發TWSE/TPEx限流的根因，改用 TWSE `MI_INDEX?type=ALLBUT0999` 單一日期批量端點（一次回傳全部1382檔股票當天完整OHLCV），只需約20-25次請求即可拼出全市場30天歷史，執行時間從20-30分鐘降到約15秒。
- **📋 新增 `DASHBOARD_DATA_SOURCE_MAP.md`**：GEX主儀表板與尋鳥戰情室前台區塊 × 交易策略參考 × 數據來源 × 稽核狀態的完整對照表，作為持久參考文件。

### 🔴🔴 v62.3 GEX 核心引擎真實選擇權未沖銷部位接軌 ✕ 5日歷史真實回補 ✕ 法人籌碼/選股雷達真數據發布版 (2026-09-14)
- **🔴🔴 最高優先級修復：`calculate_true_gex_profile()` 核心 GEX/VEX 引擎首次接上 TAIFEX 真實選擇權未沖銷部位**：
  - Self-Audit 發現：本引擎自上線以來，Call Wall / Put Wall / Zero Gamma / Max Pain / Net GEX 曲線 / GEX+ 翻轉點，全部由「真實現貨價 + 以現價為中心人工湊出的假高斯鐘形曲線」算出，選擇權籌碼本身從未接過真數據；連「週選 W1 vs W2」拆分都只是把同一數字硬乘 0.65 / 0.35。
  - 新增 `fetch_taifex_txo_open_interest()` 串接 TAIFEX 官方「選擇權每日交易行情下載」（`optDataDown`，免驗證碼，一次請求可涵蓋整段日期範圍），取得真實逐履約價 / 買賣權 / 契約月份未沖銷契約量。
  - 新增 `classify_txo_contract_buckets()` 依每個契約的**真實結算日期**（而非解析代碼字尾字母）分類週三選 / 週五選 / 月選，`build_real_option_chain()` 組裝成引擎所需格式。
  - `calculate_true_gex_profile()` 移除假高斯 fallback；W1/W2 改為各自真實到期天數獨立計算 Greeks（不再是同一數字硬拆）；履約價範圍改為「現價 ±900 內的真實上市履約價」。
  - 即時路徑（`generate_gex_payload()`）與 5 日歷史盤別的即時 GEX 曲線全面接上真實選擇權鏈；無真實數據時明確印出警告，絕不安靜退回假數字。
- **📅 `backfill_snapshots.py` 歷史快照系統補上真實 GEX 欄位，並修復 3 個從未真正抓到數據的舊 bug**：
  - `fetch_twse_index()`：原本用錯 `type=MS` 參數（回傳大盤成交統計，無指數欄位）且取值欄位錯用 `row[-1]`（空白註記欄），改為 `type=IND` + `row[1]`。
  - `fetch_taifex_daily_tx()`：原本用錯參數名 `Date_From`/`Date_To`（官方要求 `queryStartDate`/`queryEndDate`），官網直接回「日期時間錯誤」，改為正確參數名並修正 `cp950` 編碼解析。
  - `fetch_taifex_pc_ratio()`：原 URL `callPutRatioHis` 已 404，改為重用 `fetch_and_calc_vision.py` 內已驗證可用的 `fetch_official_taifex_pc_ratio()`。
  - 三個 bug 修復後，`session_snapshots.json` 的 `zero_gamma_level` / `call_wall_strike` / `put_wall_strike` / `max_pain_strike` 現在對近期交易日全部是真實計算值，取代原本永遠留白的 `null` 佔位。
- **📊 `scripts/fetch_institutional_momentum.py` 全面改為真實數據**：
  - 改抓 TAIFEX 官方 OpenAPI（`MarketDataOfMajorInstitutionalTradersGeneralBytheDate`，與 `trading room/room.js` 本身在快取失效時的即時 fallback 用同一端點，確保兩條路徑數字一致），取得真實外資 / 投信 / 自營商交易淨額與留倉淨額。
  - 5 日歷程改為 `data/momentum_snapshots.json` 真實快照逐日累積，沒有真數據的日子誠實顯示 `has_snapshot:false`，不再是每次執行都輸出同一組寫死常數。
- **🔧 `fetch_official_taifex_retail_sentiment()` 修復寫死常數**：`mtx_r_net` / `tmf_r_net`（小台 / 微台散戶淨部位）原本是寫死的 `9496` / `24932`，與旁邊算出來的真實 `long`/`short` 無關，已修正為 `long - short` 真實計算。
- **📈 `scripts/build_screener_cache.py` 選股雷達改用真實歷史 K 棒**：
  - 移除 `generate_synthetic_ohlcv()`（`random.Random(股票代號當種子)` 生成的假 30 根 K 棒歷史），改為逐檔抓取 TWSE `STOCK_DAY` / TPEx `tradingStock` 官方每日歷史，依交易日快取避免重複請求。
  - 抓不到真實歷史的股票（部分因 TWSE/TPEx 對高頻批次請求限流）明確標記 `history_unavailable:true`，訊號留空，絕不捏造。已知覆蓋率仍有改善空間，列為後續優化項目。

### 🚀 v62.2 戰情室 100% 真實數據管線 ✕ 嚴禁偽數據 Self-Audit ✕ Gemini 2.5 Flash 多模態發布版 (2026-09-12)
- **🛡️ 實裝 Hard Redline #6 最高風控鐵律**：
  - 正式寫入 [AGENTS.md](AGENTS.md) 與 [GEMINI.md](GEMINI.md)：「**嚴禁使用任何偽數據、數學隨機擬合波形或憑感覺畫圖**」。
  - 徹底移除 `fetch_market_klines.py` 與 `room.js` 中的布朗運動隨機橋接生成器與 pseudo-random 假 Tick。
- **📊 嚴格區分「價格指數 (Volume: 0)」與「期貨/個股 (真實合約/股票成交量)」**：
  - **價格指數 (加權指數 TAIEX, 櫃買指數 OTC, 美債10Y US10Y, 美元指數 DXY)**：副圖 1 成交量直方柱與 5MA/10MA 強制歸零 (`Volume: 0`)，動態標註 `⚪ 價格指數/殖利率無合約成交量 (Volume: 0)`。
  - **期貨與個股 (台指期 TXF, 微台 MTX, 小台 MXF, 台積期 CDF, 2330, 2454, 2317, 輕原油 CL)**：直連真實市場數據庫，完整呈現真實交易口數/張數與 Volume MA 5 / Volume MA 10 雙均量線。
- **🤖 Google Gemini 2.5 Flash 多模態軍師 ✕ 獨立 Key 本機安全存取**：
  - 提供專屬 API Key 設定 Modal 本地保存，直連 `gemini-2.5-flash` 享受 Google AI Studio 每日 1,500 次獨立免費額度。
  - 支援 `Ctrl+V` 貼上對帳單/TradingView 圖表截圖進行多模態形態辨識與真金白銀部位診斷。
- **🔍 股號搜尋引擎全面支援鍵盤 Enter 直接切換與 ↑/↓ 方向鍵導航**：
  - 輸入 2330、2454、TXF、台積電等代碼後直接敲 Enter，即時切換商品並連動 5 個圖表與左側 HUD。
- **📈 老墨/陳玠儒原創 XQ 籌碼指標 ✕ 雙色 ADX Pro V3 面積雲帶 1:1 對齊**：
  - Sub-Chart 4 復刻老墨價量累積動能量能柱與大戶/散戶線；ADX Pro V3 採用雙色面積雲帶與門檻水平線。
- **🛡️ 實裝 Hard Redline #5 最高風控鐵律**：
  - 正式寫入 [AGENTS.md](AGENTS.md) 與 [GEMINI.md](GEMINI.md)，強制所有 AI Agent 遵守「盤中時段一律強制走期交所 MIS 串流端點」、「盤後 Excel 強制校驗日期標頭與時間」之不可踰越紅線。
- **🔍 bump_version.py 內建方向性與矩陣健康檢查 (Sanity Audit)**：
  - 升級版本前自動檢驗：現貨大跌時期指絕不可逆向暴漲，自動防禦資料倒錯；5 日矩陣多盤別鏈路完整性 100% 通過後方允許發布。
- **📊 全站 8 大關鍵位置與數據檔案 100% 原子同步**：
  - `fetch_and_calc_vision.py`、`index.html`、`room.html`、`room.js`、`README.md`、`HISTORY.md`、`STATUS.md`、`PROJECT_HANDOVER.md`、`gex_data.json`、`embedded_data.js` 全面同步至 `v62.2`。

### 🚀 v62.1 盤中即時台指期行情串接升級與 5 日矩陣盤別價差精確校正版 (2026-09-11)
- **⚡ 盤中即時台指期 (TXF) 3 階行情引擎架構建立**：
  - 徹底解決原先在開盤中（08:45 ~ 13:45）因讀取期交所日盤盤後 Excel (`futDailyMarketExcel?marketCode=0` 在未結算前為昨日數據) 導致當日行情抓到昨日結算價、盤別漲跌符號顛倒的關鍵問題。
  - 導入期交所 MIS 實時串流報價 API (`https://mis.taifex.com.tw/futures/api/getQuoteList`)，盤中自動精準抓取當月大台期貨 (`TXFI6-F`) 即時行情、成交點數與漲跌幅。
- **📊 5 日結構歷程矩陣盤別 Shift 精準連續計算**：
  - `T-1 日盤 (9/10)`：精確記錄昨日日盤結算價 `46,870` 點。
  - `T-1 夜盤 (9/10 夜)`：精確記錄昨夜盤結算收盤價 `46,072` 點，價差精確計算為 `-798 點`。
  - `T 日盤 (9/11 Live 即時動態)`：即時串接即時大台報價（`45,966` 點），與昨夜盤價差精確對齊為 `-106 點`（較昨盤日盤下跌 `-904 點`），GEX 護盤牆與 Zero Gamma 全面動態精確校正。
- **🌪️ CBOE 美股 ^VVIX (波動率之波動率) 實時採集與 4 級尾部風險矩陣 (Tail Risk Matrix)**：
  - 串接 Yahoo Finance API 實時採集美股 `^VVIX`（即時值 `102.66` / 漲跌 `+1.85` / 幅度 `+1.83%`），解析做市商對沖黑天鵝風險的買盤加速度。
  - 建立 4 級尾部風險評級與做市商賣腳安全氣墊指引：
    1. `VVIX < 95` 🟢 **風穩常態**：賣腳防守安全氣墊 `250~350 點`，貼牆收租。
    2. `95 <= VVIX < 100` 🟡 **避險溫和升溫**：賣腳防守氣墊預備擴大至 `300~400 點`。
    3. `100 <= VVIX < 110` 🟠 **尾部黑天鵝避險潮 (當前 102.66)**：做市商/大資金大量買進 VIX Calls 避險！賣腳防守氣墊**強制擴大至現價 350~500 點外**，啟動 B 軌金字塔階梯伏擊網。
    4. `VVIX >= 110` 🔴 **極端波動暴衝**：氣墊 `500+ 點`，**嚴禁近端裸賣**。
- **⚡ 隱含波動率加速度背離 (VIX/VVIX Divergence) 實時診斷**：
  - 當 VIX 處於低檔 (如 VIX < 20)，但 VVIX 率先突破 100+ (如 102.66)，發動「⚠️ 隱含波動率加速度背離」警報：顯示大資金正在提前爆買 VIX 深價外 Call 進行尾部避險，大盤防守牆韌性轉弱。
- **🌐 GEX 主儀表板 Card 9 恐慌 & 避險雷達區塊擴充**：
  - 新增 `🇺🇸 美股 ^VVIX (波動加速度)` 實時欄位、`stat-vvix-regime-badge` 狀態標籤與 `stat-vvix-safety-buffer` 賣腳氣墊防禦提示。
- **🦅 尋鳥戰情室 (Trading Room) 4 欄式 Macro Risk HUD 整合**：
  - 頂部 Quick Pills 增設 `🌪️ VVIX 102.66 🟠`。
  - 左側 Macro Risk HUD 升級為 4 欄式宏觀風險雷達（DXY, US10Y, VIX, VVIX），連動 `risk-badge-vvix` 與整體 `🟠 尾部避險` 警報。
- **🇺🇸 台灣 6 檔正統在美 ADR (Level 2/3) 專屬換股公式與折溢價精確算法**：
  - 嚴格區分「正統交易所上市 ADR」與「OTC 場外粉紅單/美股直接掛牌雜訊」，確立全市場官方核心 6 檔 ADR：
    1. **台積電 (`TSM`)**：換股比 $1:5$ ➔ $\text{台股換算價} = \text{TSM} \div 5 \times \text{匯率}$
    2. **聯電 (`UMC`)**：換股比 $1:5$ ➔ $\text{台股換算價} = \text{UMC} \div 5 \times \text{匯率}$
    3. **日月光投控 (`ASX`)**：換股比 $1:2$ ➔ $\text{台股換算價} = \text{ASX} \div 2 \times \text{匯率}$
    4. **中華電信 (`CHT`)**：換股比 $1:10$ ➔ $\text{台股換算價} = \text{CHT} \div 10 \times \text{匯率}$
    5. **友達光電 (`AUO`)**：換股比 $1:10$ ➔ $\text{台股換算價} = \text{AUO} \div 10 \times \text{匯率}$
    6. **南茂科技 (`IMOS`)**：換股比 $1:20$ ➔ $\text{台股換算價} = \text{IMOS} \div 20 \times \text{匯率}$
  - 即時計算折溢價公式：$\text{折溢價率 (\%)} = (\frac{\text{ADR 換算台股價}}{\text{台股現貨收盤價}} - 1) \times 100\%$。
- **📱 05:00 清晨 Telegram 盤後快訊與 GEX 網頁夜盤連動品質提升**：
  - 自動提取當日夜盤交易中之 ADR 收盤價與即時匯率，清晰標示「對應台股代號、台股中文名稱、換算台股合理價與折溢價百分比」，提供最嚴謹無雜訊的開盤前夕指引。
| **`v51.0`** | 2026-09-09 | 🦅 **富邦 API 實時行情極速專線直連、三大指數左上角戰情卡片、選股雷達1400+全台股真實報價與雙向無縫導航版** |

---

## 🎯 各版本詳細更新紀錄

### 🚀 v60.0 台指散戶多空比、前五大特法分歧診斷、夜盤個股期ADR連動與投信波段認養快選發布版 (2026-09-10)
- **👥 小台散戶多空比 (Retail Mini Ratio) ✕ 前五大特法避險分歧診斷 (Top 5 Traders Divergence)**：
  - 首頁即時情緒面板新增「小台散戶多空比」經典反指標（公式：$-(三大法人小台留倉) / 小台總OI$），並依 $>+30\%$ 散戶死多 (極易下殺) / $<-30\%$ 散戶放空 (極易軋空) 警示。
  - 導入期交所官方「前五大特定法人」大額未平倉與外資淨部位交叉比對：外資淨空單創高但特法多單大增時，標註「🟡 避險套利分歧 (切勿盲目追空)」。
- **🇺🇸 夜盤 6 檔個股/ETF 期貨連動美股 ADR 即時行情**：
  - 台積電期貨連動 TSM ADR、聯電期貨連動 UMC ADR、台指期連動 MSCI 台灣 ETF (EWT) 與美債 ETF (TLT)，並於卡片直接顯示即時溢價率與美股盤前漲跌幅。
- **🚀 投信波段認養 (IT Adoption Ratio) 矩陣與選股雷達快選**：
  - 於全市場個股期貨矩陣新增「投信認養佔比」欄位與類別快選按鈕（判定標準：投信買超佔股本比 $>0.5\%$ 且連續買超 $\ge 3$ 天）。
  - 戰情交易室選股雷達新增「⭐ 投信波段認養 ＋ Grade A」一鍵快選與籌碼自選勾選條件。
- **⏱️ 台股全市場盤後數據公布時程全覽建立 ([MARKET_DATA_SCHEDULE.md](MARKET_DATA_SCHEDULE.md))**：
  - 完整梳理 13:30 收盤至隔日 05:00 夜盤收盤之官方數據公布流水線（15:30 三大法人現貨/期權、18:00 大額交易人、18:30 券商分點、20:00 官股行庫、21:00 融資維持率、05:00 夜盤行情）。
  - 確立「隔日 05:00 清晨快訊」為全市場 100% 完整籌碼匯總黃金時段。
- **🎯 四大主力商品（台指期、選擇權、個股期、個股）量化優化升級**：
  - **台指期**：確立小台散戶多空比反指標、前五大特法 vs 外資空單避險分歧診斷。
  - **選擇權**：正/負 Gamma 環境制度化風控（+GEX 雙賣收 $\theta$、-GEX 防追殺順勢/縮倉）、動態 Call/Put Wall 搬移預警、週選嚴禁拆單鐵律。
  - **個股期貨**：直擊期交稅低 150 倍之主力大戶足跡，結合 17:30 大額交易人未平倉 + 05:00 夜盤 6 檔美股連動行情。
  - **個股現貨**：投信連買佔股本比認養策略 + 八大官股逆勢護盤指標 + 個股融資維持率斷頭警戒。
- **🏛️ 戰情交易室 (Bird Trading Room) 核心與行動端完整升級**：
  - **📐 100% 縮放視窗溢出與副圖裁切修復**：修復主圖與 4 大副圖 (全 5 圖連動) 在 100% 縮放時被底部裁切與右側留白溢出問題，完美鎖定 100vh 全螢幕。
  - **📱 手機行動端抽屜式排版 (Mobile Responsive Drawers)**：針對手機與平板視窗 (<1024px / 420px) 實作側邊欄抽屜化，點擊頂部 `📊 行情` 與 `🤖 AI軍師` 鈕平滑滑入，中心 5 圖保持 100% 寬度清晰顯示。
  - **🇹🇼 嚴格遵循台灣習慣「紅漲綠跌」配色標準**：修正報價卡與價差標籤配色，上漲一律為台灣紅 (`--call-color #ff4757`)，下跌一律為台灣綠 (`--put-color #2ed573`)。
  - **🎯 頂部 8 大精選核心資產快速切換 (Core Preset Assets)**：整合 `台指期 TXF`、`加權指數 TAIEX`、`櫃買指數 OTC`、`台積期 CDF`、`微台 MTX`、`小台 MXF`、`美債10Y US10Y`、`美元指數 DXY`、`紐約輕原油 CL`，一鍵即時切換走勢並適配公債殖利率/商品價格尺度。
  - **🌐 全球宏觀風險雷達精簡整合至左側邊欄頂部**：將 DXY 美元指數、US10Y 美債殖利率與 CBOE VIX 整合為極簡 HUD 懸浮卡片，放置於三大即時指數上方，並與富邦 API 即時 Tick 脈動跳動同步。
  - **📊 真實 K 線與技術指標數學引擎重構**：全面剔除偽造的正弦波模擬與亂數訊號，改以真實錨定市價與真實波動率為基礎，嚴格按數學公式即時計算 Multi-MA (7/17/88/200)、SMMA(200)、VWAP ±1σ、戰情雙層 MACD (4 色柱體動能加速)、波段拐點 CCI(20 買賣轉折點)、AO 震盪、DMI/ADX(14) 與神奇九轉 (DeMark 9★ 抄底/逃頂 Setup 與 13★ 倒數)，並將動能鳥訊號嚴格限制在多因子放量突破條件下觸發，杜絕訊號濫發。
  - **🤖 AI 量化軍師完全進化 (Gemini Pro ✕ AGENTS.md 實盤持倉體檢與連續洗價單)**：
    - 徹底淘汰一問一答死板模板，升級為具備通用 Gemini 思考與推理能力之即時軍師。
    - 支援**自然語言持倉部位識別**（例如輸入 `W2 47000 SP 2口 @ 65, 46900 BP 2口 @ 35`），即時解析履約價、計算 Sell Leg 安全防守距離、Delta 估算、風報比（Max Profit / Max Loss）。
    - 深度內嵌 `AGENTS.md` 最高風控鐵律：**嚴格警示週選垂直價差單「嚴禁拆單 (No Legging Out)」**（防止解鎖為無限風險釀成多空雙巴），並在暴衝急殺時嚴禁追價。
    - 自動產出**券商連續洗價單實盤設定指南 (IOC 市價/對手價雙腳同退)**，給出精確停損觸發點位與執行參數。
- **🌙 6 大夜盤個股/ETF期貨矩陣數據真實度清洗**：
  - 清理夜盤卡片中非官方之夜盤法人合成估算欄位，改以清晰標示「籌碼意圖」與真實夜盤價量基差，杜絕任何誤導。
- **📊 8 大精準主題資金輪動矩陣意圖整合**：
  - 資金輪動矩陣狀態判定深度結合板塊成分股主力意圖標籤，即時呈現「買盤點火狂拉」、「避險對沖防守」與「賣壓調節拉回」之深度動能。
| **`v50.7`** | 2026-09-07 | 🏛️ **TWSE 融資維持率發布狀態日期比對與開盤未公布狀態判定校正版** |
| **`v50.6`** | 2026-09-03 | 🧲 **Max Pain 4 大空間幾何拓撲 ✕ 3 大籌碼強度 二維共振 12 種全情境實戰矩陣版** |
| **`v50.5`** | 2026-09-03 | 🦅 **台指選擇權造市商 21 章量化實戰手冊、5口微台 Covered Call 動態避險與 100% 現貨養老完全體版** |
| **`v50.4`** | 2026-09-03 | 📅 **方案 B：本週重大市場焦點週報 (全自動排程引擎上線版)** |
| **`v50.3`** | 2026-09-03 | ⚡ **VIX 5日歷史歷程欄位、微觀結構恐慌警報與 HTML DOM 結構終極修復版** |
| **`v50.2`** | 2026-09-02 | 📊 **富邦期貨本週市場焦點 (2026.08.31-09.04)、熱門股票期貨對照矩陣與 VIX 雙軌對策版** |
| **`v50.1`** | 2026-09-02 | ⚡ **VIX 雙軌數據引擎 (TAIFEX VIX ✕ 美股 ^VIX)、波動率四級評級與 GEX ✕ VIX 實戰對策手冊發布版** |
| **`v49.4`** | 2026-09-02 | ⚡ **富邦 API 實時報價 WebSocket 閃爍跳動與 Zero Gamma 即時複利漂移雙重熱修復版** |
| **`v49.3`** | 2026-09-01 | 🛡️ **TWSE 現股報價雙軌熱備援與通行碼彈窗自動通關熱修復版** |
| **`v49.2`** | 2026-09-01 | ⏰ **GitHub Actions 離峰 Cron 排程錯開、離線夜盤時段視窗修復與 TWSE 融資維持率判定校正版** |
| **`v49.1`** | 2026-08-29 | 🕒 **動態 4 階段 Session 配對架構、期交所官方夜盤個股期 API 直連與 TWSE 融資維持率 API 版** |
| **`v49.0`** | 2026-08-28 | 📱 **Instagram & Threads 官方 QR Code 增粉與 @bluebird_finder 標籤全站整合版** |
| **`v48.1`** | 2026-08-28 | 🚨 **國際重大總經事件、富台/MSCI 甩尾與期權結算日 實時避險防護雷達版** |
| **`v48.0`** | 2026-08-28 | 🌙 **期交所官方 6 大夜盤個股/ETF期貨 價量與籌碼即時行情矩陣版** |
| **`v47.9`** | 2026-08-28 | 🌙 **夜盤 05:00 收盤價精確校正與自動排程時區防錯版** |
| **`v47.8`** | 2026-08-28 | 🤖 **Gemini AI 焦點掃描卡與頂部 KPI 數據動態 100% 同步版** |
| **`v47.7`** | 2026-08-27 | 📱 **iOS Safari 同步觸控手勢下載防護修復版** |
| **`v47.6`** | 2026-08-27 | 📸 **社群圖卡即時重繪與 HTTP Cache-Buster 破壞舊快取防護版** |
| **`v47.5`** | 2026-08-27 | 📱 **手機版行動裝置多圖下載與跨平台 ZIP 智慧自動包裝修復版** |

---

## 🎯 各版本詳細更新紀錄

### 🚀 v51.0 富邦 API 實時行情極速專線直連與多維選股雷達真實數據整合版 (2026-09-09)
- **🦅 富邦 API (Fubon Neo SDK) 實時報價極速專線整合**：
  - 成功直連富邦 Neo API SDK 進行證券與期貨實時報價，解決清晨盤後缺口（05:00~08:45）自動切換備援，輸出 `🟢 富邦 API (Live)` 實時狀態。
  - 建立背景實時 Gateway 服務 (`scripts/live_price_server.py`)，每 1.5 秒更新一次台指期 (TXF)、加權指數 (TAIEX) 與櫃買指數 (OTC)。
- **⚡ 三大指數即時行情卡片 (Triple Index Real-time HUD Card)**：
  - 在戰情室 (`room.html`) 左上角重塑 `⚡ 三大指數即時行情` 卡片，加權 (TAIEX)、櫃買 (OTC) 與台指期 (TXF) 報價一目了然，完全解決跑版切字問題。
- **🔍 1,423 檔全台股選股雷達 (Quant Screener) 真實報價庫**：
  - 整合 TWSE/TPEx Open API 抓取 7,141 筆全市場股票真實日收盤與成交量 (`tw_quotes_latest.json`)，華新 (1605 = 37.90 元) 等全場個股價格 100% 準確無誤。
  - 預算 8 大指標多重選股策略 (火箭、藍鳥、飛碟、閃電、雙層 MACD 水下金叉、5K 創高、DeMark 9★)。
- **🔄 雙向無縫切換導航 (Bidirectional Seamless Navigation)**：
  - 在 `index.html` 頂部新增發光 `🦅 進入戰情室` 按鈕，與 `room.html` 的 `📊 返回宏觀儀表板` 實現雙向秒切。

### 🚀 v50.9 5 日歷程矩陣日夜盤數據無縫對齊與交易室戰情架構規劃版 (2026-09-08)
- **📈 5 日歷程矩陣日夜盤歷史數據對齊修復 (History Matrix Synchronization Fix)**：
  - 徹底修復日盤開盤後（08:45~13:45）因時間判斷過濾條件導致「今天清晨 05:00 剛結算之夜盤（47,329 點）」從表格消失、直接跳到 mock 偏移資料之問題。
  - 將歷史陣列中的 `t1_night` 與 `t0_day` 數據嚴格綁定至當前即時運算之 `session_shift` 與 `gex_profile`，確保加權、台指期、Zero Gamma、Call Wall、Put Wall、Max Pain 上下 100% 絕對一致。
- **🦅 尋鳥交易室戰情架構規劃落地 ([TRADING_ROOM_ARCH_PLAN.md](TRADING_ROOM_ARCH_PLAN.md))**：
  - 引進老墨 Lightweight Charts v5 戰情室架構，確立 TV Pine Script 指標 1:1 轉寫 Python 計算方案。
  - 確立同專案「雙頁面模式」(`index.html` 宏觀儀表板 + `room.html` 滿版三欄即時戰情室) 與 Model-Agnostic AI 軍師（Gemini 主力 / Claude / Hermes 彈性轉接）架構。
- **🔒 私有化部署與商業化訂閱遷移方案確立**：
  - 規劃 Cloudflare Pages + Worker 搭配 JWT 會員金流驗證路徑，保護核心量化指標與數據資產。

### 🚀 v50.8 Playwright 2K 高清圖卡自動截圖管線與 GitHub Pages 雙向自動化同步修復版 (2026-09-07)
- **📸 2K 高清社群圖卡自動截圖硬核管線**：
  - 建立基於 Playwright 的 2K 高清社群圖卡自動截圖管線，結合哈希與時間戳硬核過濾。
  - 修復 GitHub Actions 雙向自動化資料同步流程。

### 🚀 v50.7 TWSE 融資維持率發布狀態日期比對與開盤未公布狀態判定校正版 (2026-09-07)
- **🏛️ TWSE 融資維持率發布狀態日期嚴格校驗 (`fetch_twse_margin_maintenance`)**：
  - 修正原判定邏輯僅檢查 API 回傳成功 (`stat == 'OK'`) 而忽略 `pub_date` 之問題。
  - 改為嚴格校驗 `is_published = (pub_date == target_date_str)`，開盤至盤後 20:30 前正確呈現「`未公布 (約20:30~21:00實時連線)`」，徹底解決開盤早鳥提前顯示前日數值之時間差漏洞。

### 🚀 v50.6 Max Pain 4 大空間幾何拓撲 ✕ 3 大籌碼強度 二維共振 12 種全情境實戰矩陣版 (2026-09-03)
- **🧲 Max Pain 空間拓撲二維共振升級 (2D Spatial ✕ Institutional Resonance)**：
  - 徹底解決「大跌行情卻死板判定多頭軋空」之認知矛盾，將市場拓撲升級為 4 大幾何型態：
    - 🔴 **型態 A【痛點沉底 / 懸空防守拓撲】** (`Max Pain < Put Wall < Call Wall`)：痛點沉在低位，近端 Put Wall 建立防守線。若籌碼偏多 (Level +1/+2) 為踩牆軋空；若籌碼偏空 (Level -1/-2) 則 Put Wall 脆弱，一旦跌破將被下方 Max Pain 引力磁吸加速下殺！
    - 🟡 **型態 B【對稱健康箱體拓撲】** (`Put Wall <= Max Pain <= Call Wall`)：對稱箱體，週三強烈吸附，Iron Condor 鐵鷹全收。
    - 🟢 **型態 C【恐慌避險 / 下檔開天窗拓撲】** (`Put Wall << Max Pain ≈ Call Wall`)：下檔支撐真空開天窗，Bear Call Spread 空差防禦。
    - 🚀 **型態 D【極端軋空 / 痛點頂天拓撲】** (`Put Wall < Call Wall < Max Pain`)：痛點超越 Call Wall，做市商雙重 Gamma 擠壓，順勢做多。
- **📊 12 種二維共振實戰矩陣全面收錄**：
  - 在 `index.html` 拓撲診斷對照區塊新增 12 種全情境矩陣表（A1 踩牆軋空、A2 懸空震盪、A3 假支撐磁吸、B1~B3、C1 恐慌 V 轉、C3 順風避險大崩盤、D1 歷史級破裂軋空、D3 籌碼背離殺多等）。
  - `app.js` 動態讀取法人多空強度，在 Tooltip 即時提示對應共振代號與實戰 SOP！
  - 獨立手冊 `docs/OPTIONS_QUANT_PLAYBOOK.md` 同步擴充「第 4.5 節：二維共振 12 種全情境實戰矩陣」。

### 🚀 v50.5 台指選擇權造市商 21 章量化實戰手冊、5口微台 Covered Call 動態避險與 100% 現貨養老完全體版 (2026-09-03)
- **🦅 21 大章節造市商量化操盤手冊全面收錄**：
  - 在儀表板 `Card 9` 頂部的 `📖 VIX 教學` 彈窗與獨立文件 `docs/OPTIONS_QUANT_PLAYBOOK.md` 中，收錄完整 21 大章節法人造市商級實戰操盤心法：
    - **第 1~4 章**：GEX ✕ VEX ✕ VIX 定義、台灣自營商造市避險機制、四大共振矩陣、VIX 四級燈號。
    - **第 5~7 章**：週三/週五選鐵兀鷹戰術、週三 15:00 速度戰 ✕ 週一 09:30 陣地戰、單邊停利與 VIX 頂點反手補血。
    - **第 8~10 章**：IOC 連續洗價單架構、GEX 牆體 ✕ VIX 2σ 步長共振、階梯式金字塔口數下法與長假跳空防禦。
    - **第 11~13 章**：黑天鵝尾部保護 (Tail Hedge Ratio Spread)、VIX < 13 極端低波三大變形、VIX > 18~22+ 高隱波反手倒貨三步收割法。
    - **第 14~17 章**：一分鐘百點斷路器、總價差洗價、死士主力雙軌制、組合單讓點保證成交、富邦犀利 2.0 中價洗價與一鍵抽單 Roll 倉。
    - **第 18~21 章**：5口微台 Covered Call 跨資產動態 Delta 對沖、Covered Call 4 大變形 (PMCC/動態避險/Covered Straddle/Ratio)、輝哥 100% 現貨本位不開槓桿週週收息 SOP、兩階段修煉全圖譜。
- **📱 跨平台策略手冊獨立導出**：
  - 自動生成格式清爽的 `docs/OPTIONS_QUANT_PLAYBOOK.md`，方便隨時匯出給 Lumi 機器人進行策略建構與自動化回測。
- **⚙️ 全系統版本標籤升級**：
  - 儀表板 HTML 頂部 Badge 升級為 `TXO GEX 量化系統 v50.5`，腳本與快取破壞參數統一升級為 `v50.5`。

### 🚀 v49.4 富邦 API 實時報價 WebSocket 閃爍跳動與 Zero Gamma 即時複利漂移雙重熱修復版 (2026-09-02)
- **頂部連線狀態標籤閃爍跳動熱修復 (Status Pill Anti-Flicker Fix)**：
  - 修復 `app.js` 中 `updateMarketTradingStatus()` 每 2 秒強制重洗狀態文字與 `handleLiveTick()` 傳入 `provider_name` 互相搶奪蓋掉標籤文字的衝突，補齊 `feedText.dataset.hasLiveSocket = 'true'` 連線鎖定旗標。
- **Zero Gamma 與 GEX+ Flip 即時計算複利累加漂移修復 (Zero Gamma Accumulation Drift Fix)**：
  - 解決高頻 Tick（富邦 API WebSocket / HTTP 輪詢）觸發時，`liveZg` 不斷在已修改過的 `gexData.zero_gamma_level` 上重複加減價格價差的複利累加 BUG（避免點位一路扣至 `-24,436.6` 等無窮負數點）。
  - 引入 `_base_zero_gamma` 與 `_base_gex_plus_flip` 靜態凍結記憶體，確保實時點位算式精準依據官方日/夜盤結算點位進行單次動態校正。
- **GEX 圖表 Y 軸防爆邊界保護網 (Plotly Chart Safety Bounds Guard)**：
  - 針對 `renderGEXChart()` 增加 `> 10000` 數值門檻防護，避免異常點位破壞 Plotly Y 軸（履約價 Strike）刻度，確保履約價柱狀圖與線型絕不擠壓變形。

### 🚀 v49.3 TWSE 現股報價雙軌熱備援與通行碼彈窗自動通關熱修復版 (2026-09-01)
- **夜盤真實成交量 (Night Real Volume) 對齊**：
  - 升級 etch_taifex_official_night_stock_futures()，從期交所 utDailyMarketExcel?marketCode=1 官方夜盤端點精準提取 cols[8] 夜盤真實成交量與 cols[6] 漲跌點數。
  - 徹底排除將日盤總成交量誤寫入夜盤之問題，全系統 6 大夜盤股期/ETF成交量對齊期交所官方公告。
- **4-Tier 熔斷防範與 0 價避險保護網**：
  - 強化 etch_and_calc_vision.py 數據生成器，確保期交所清晨 05:00~06:15 結算維護期不落入空值，寫入無零價保證。


### 🚀 v49.1 動態 4 階段 Session 配對架構、期交所官方夜盤個股期 API 直連與 TWSE 融資維持率 (2026-08-29)
- **動態 4 階段 Session 配對架構 (4-Phase Dynamic Session Architecture)**：
  - 劃分 `DAY_LIVE` / `DAY_SETTLED` / `NIGHT_LIVE` / `NIGHT_SETTLED` 4 大時段。
  - 當前行情 (`active_price`) 嚴格配對當前階段之 Zero Gamma (`zg`)、Call Wall (`cw`)、Put Wall (`pw`) 與 Max Pain (`mp`)，觸發 Put Wall 跌破警示與 Max Pain (45,700 點) 磁吸防守。
- **期交所官方 6 大夜盤個股/ETF期貨 API 直連 (`marketCode=1`)**：
  - 直連 TAIFEX `marketCode=1` API，動態取得聯電期 (`2303` `127.00`)、台積電期 (`2330` `2408.00`)、小型台積期 (`2330F` `2408.00`)、元大台灣50期 (`0050` `106.60`)、小型50期 (`0050F` `106.60`) 及元大美債20年期 (`00679B` `25.88`) 之夜盤收盤定案價。
  - 保留 TWSE 日盤現貨收盤價 (聯電 `130.00`) 與 TAIFEX 夜盤期貨價 (聯電期 `127.00`)，真術驗算並渲染最新夜盤逆價差 (`-3.00 點`)。
- **TWSE 信用交易融資維持率 API 實時動態連線 (`MI_MARGN`)**：
  - 數據引擎連線 TWSE 統計 API，當證交所完成當日盤後清算（約 20:30），自動轉為 `is_published = True` 並寫入當日大盤融資維持率 (`160.6% 🟢 安定`) 與個股維持率 (`145.9%`)。
- **常駐防錯發布 SOP 規則 (`.agents/rules/VERSION_RELEASE.md`)**：
  - 建立常駐 release SOP，強制於 git commit 前重跑腳本重繪 Payload，徹底消弭版號落差與靜態動態快照閃跳問題。

### 🚀 v49.0 數據引擎 Self-Audit 重構、多源熱備援與 21:00 融資維持率排程補齊 (2026-08-28)
- **TWSE 現貨指數多源熱備援 (`fetch_twse_realtime_indices`)**：引入 3 級熱備援（TWSE MIS ➔ Yahoo Finance `^TWII`/`^TWOII` ➔ `gex_data.json`），徹底消除 GitHub Actions 國外 IP 遭 TWSE 阻擋降級成舊值 `45811.01` / `400.95` 的硬碼問題。現貨加權精準抓取為 `46,331.45 (+356.23)`，櫃買指數為 `402.83 (+2.45)`。
- **現貨漲跌幅實時動態渲染**：清理 `index.html` 硬碼 `+3,186.45` 字串，於 `app.js` 動態更新 `#stat-spot-sub` 與 `#stat-otc-sub` 漲跌點數與 %，並於 `handleLiveTick` 加入 ticker 報價分流。
- **5 日歷程矩陣 T-1 前一交易日收盤價真實比對**：歷史 10 盤對齊真實前一交易日現貨收盤價 (`prev_day_spot = spot_price - spot_change`)，讓矩陣真實算出 `(+356.23)` 點與 `(+2.45)` 點，解決手動提早或多次觸發腳本時顯示偽造 `(+74.00)` 點之錯誤。
- **GitHub Actions 補齊 21:00 TWD 融資維持率定時排程 (`auto_update.yml`)**：新增 `- cron: '0 13 * * 1-5'` (21:00 TWD)，在證交所每晚 21:00 公布「大盤整戶融資維持率」時自動補齊數據，且安全保護 15:30 已定案之 GEX 基線與籌碼。
- **全站版本號 100% 對齊 (消除重整閃爍)**：同步 `ENGINE_VERSION = "v49.0"`，解決 HTML (`v49.0`) 與 JS 讀取數據 (`v48.2`) 不一致導致的重整瞬間閃爍跳動現象。

### 🚀 v49.0 Instagram & Threads 官方 QR Code 增粉與 @bluebird_finder 標籤全站整合 (2026-08-28)
- **獨立高解析度 QR Code 自動生成器 (`scripts/generate_qr_codes.py`)**：自動產出高對比、相容所有手機鏡頭與第三方 APP 秒讀之 Instagram (`assets/qr_instagram.png`) 與 Threads (`assets/qr_threads.png`) 專屬黑金科技風 QR Code。
- **QR Code 下方標示官方 ID (`@bluebird_finder`)**：於生成之 QR Code 圖檔、1:1 社群懶人圖卡與 Web 儀表板頁尾清楚標示 `IG: @bluebird_finder` 與 `Threads: @bluebird_finder`。
- **1:1 正方形社群懶人圖卡頁尾整合 (`scripts/generate_social_card.py`)**：每張生成的 1:1 正方形圖卡（Card 1/2/3）頁尾加入雙平台 QR Code、官方 ID 標籤與吸粉文案。
- **Web Dashboard 導覽列與頁尾吸粉卡片 (`index.html` & `style.css`)**：頂部 Nav 增設 `📱 追蹤社群 (@bluebird_finder)` 按鈕，頁尾增設暗黑玻璃風格 `social-follow-card`。

### 🚀 v48.1 國際重大總經事件、富台/MSCI 甩尾與結算日 實時避險防護雷達 (2026-08-28)
- **實時避險倒數雷達 (`#macro-events-radar-panel`)**：
  - 完整整合 **週/月選大結算**、**SGX 富台指結算**、**MSCI 季度/半年度甩尾調整**、**美大非農 (NFP) + 失業率**、**美 ADP 小非農**、**美每週初領失業金 (Jobless Claims)**、**美 CPI 通膨數據** 與 **美聯儲 FOMC**。
  - **Lumi TG 機器人規格 100% 對齊 (`pattern_type`)**：區分 `POINT_TIME` (定點數據型) 與 `WINDOW_TIME` (視窗洗盤型)，精確提示發布前流動性抽離或尾盤撮合擺盪。
  - **客製化提早預警視窗 (Tailored Lead Times)**：各事件獨立設定 `warning_lead_hours` 與 `critical_lead_mins`（如 CPI/NFP 24h警戒/120m告急；小數據 6h警戒/30m告急）。
  - **時區防錯與夏冬令自動校正**：全站顯示統一為 `(台灣時間)`，全球絕對 Unix Epoch ms 時間戳確保秒級倒數零誤差。
  - **接續 5 大重磅總經日曆矩陣**：視覺化預覽一週催化劑並提供 GEX 對沖操作提醒。

### 🚀 v48.0 官方 6 大夜盤股期/ETF期 即時價量關係與指標導航 (2026-08-28)
- **夜盤 6 大標的熱力面板 (`#night-six-spotlight-container`)**：
  - 在個股期貨篩選器頂部新增獨立卡片面板，即時聚焦台積電期 (`2330`)、小型台積期 (`2330F`)、元大台灣50期 (`0050`)、小型台50期 (`0050F`)、聯電期 (`2303`) 及元大美債20年期 (`00679B`)。
  - **熱門度優先級排序**：卡片與表格預設嚴格依據 `2330 (台積期)` ➔ `2330F (小台積期)` ➔ `0050 (台50期)` ➔ `0050F (小台50期)` ➔ `2303 (聯電期)` ➔ `00679B (美債期)` 排序。
  - **雙重浮水印防護**：於產業資金輪動 Banner 與 6 大夜盤價量矩陣 Banner 右下角補齊高對比度 **`© 尋鳥 Bluebird Finder`** 浮水印。
  - **指標整合**：現價/期價、正逆價差診斷、漲跌幅、大盤點數貢獻 (Point Contribution)、價量動能訊號 (`🔥價量齊揚`/`⚠️帶量拉回`)、外資與自營商未平倉部位及 TWSE 除權息日程。
  - 下拉選單新增 `🌙 官方 6 大夜盤個股/ETF期貨 (Night-Traded 6)` 一鍵快速篩選。

### 🚀 v47.9 夜盤 05:00 收盤價精確校正與自動排程時區防錯 (2026-08-28)
- **夜盤定案收盤價校正 (`fetch_and_calc_vision.py`)**：
  - 將台指期夜盤 (TXF) 結算價精確校正至期交所官方 05:00 收盤價 `46,388` 點，聯動重計算最新 Call Wall (46,700 / 46,200), Zero Gamma (46,517.9 / 46,017.9), Put Wall (46,300 / 45,800) 與 Max Pain。
- **自動排程時區對齊與防錯 (`scripts/fetch_and_calc_vision.py`)**：
  - 優化夜盤時間視窗判定 `(3 <= now_hour < 12)`，解決早晨 03:00~04:00 清晨執行時時區未覆蓋問題。
  - `index.html` 靜態資源全數更新為 `?v=v47.9`。

### 🚀 v47.8 Gemini AI 焦點掃描卡與頂部 KPI 數據 100% 動態同步 (2026-08-28)
- **Gemini AI 摘要動態綁定 (`app.js`)**：
  - 修復底部 `Gemini AI 籌碼、價差與除權息事件量化焦點掃描` 卡片呈現硬編碼舊數據，與頂部動態 KPI 卡片/切換頁籤數據不符的問題。
  - 在 `populateAiQuantDigest()` 中動態帶入當前活躍 Session 之指數現價/夜盤價、Zero Gamma、Call Wall 與 Put Wall，確保 AI 摘要與儀表板頂部 100% 完全動態齊平！
  - `index.html` 靜態資源更新為 `?v=v47.8`。

### 🚀 v47.7 iOS Safari 同步觸控手勢下載防護 (2026-08-27)
- **消除 `await fetch()` 非同步阻斷 (`app.js`)**：
  - 將 `downloadSingleCard()` 重構為 100% 純同步觸發，避免 iOS Safari 在異步等待後丟失「使用者點擊上下文 (User Gesture Stack)」而默默攔截下載的致命問題。
  - Modal 內新增『💡 手機用戶小貼士』引導長按加入相簿。

### 🚀 v47.6 社群圖卡即時重繪與 HTTP Cache-Buster 防快取 (2026-08-27)
- **圖卡即時重新渲染與 HTTP 防快取機制 (`app.js`)**：
  - **圖卡即時重繪**：執行 `python scripts/fetch_and_calc_vision.py` 重新生成全套 3 張社群圖卡 (P1, P2, P3)。
  - **HTTP/CDN Cache-Buster 強制破壞快取**：為所有下載請求注入動態時間戳記 (`?t=${Date.now()}`) 與 `{ cache: 'no-cache' }` 標頭，解決瀏覽器讀取 08:00 AM 舊圖快取的困擾。

### 🚀 v47.5 手機版行動裝置多圖下載與 ZIP 智慧自動包裝修復 (2026-08-27)
- **手機雙核心防護機制 (`app.js`)**：
  - **手機版一鍵自動 ZIP 打包**：針對 iOS Safari / Android Chrome 彈出式 Prompt 會吃掉中間圖卡 (P2) 的現象，行動裝置點擊「🚀 一鍵下載全部 3 張 PNG」時自動使用 `JSZip` 一鍵打包成單一 `.zip` 檔，單次點擊 100% 收齊 3 張圖。
  - **下載間隔與 Blob 延長**：序列化下載間隔拉長至 1500ms，`URL.revokeObjectURL` 延長至 15 秒，確保行動裝置瀏覽器能夠完整將二進位檔存入硬碟。

### 🚀 v47.4 IG/Threads 社群圖卡一鍵下載與 Blob 零阻檔防護 (2026-08-27)
- **社群圖卡一鍵下載 Blob 零阻檔防護 (`app.js`)**：
  - **移除 `target="_blank"` 衝突**：重構 `downloadSingleCard()` 絕不添加 `target="_blank"`，解決 Chrome / Edge 彈出式視窗攔截器 (Popup Blocker) 將第一張圖卡 (`P1_Overview.png`) 誤判為腳本彈窗而阻檔下載的問題。
  - **內存 Blob 物件下載**：透過 `fetch()` 將圖卡轉為 `Blob` 物件並綁定 `URL.createObjectURL`，100% 強制瀏覽器發起原生檔案下載。
  - **序列化 Async/Await 佇列下載**：將「🚀 一鍵下載全部 3 張 PNG」改為 `async/await` 順序流觸發（每張間隔 500ms），徹底克服瀏覽器多檔案下載防護機制 (Multiple Download Warning)，保證 P1、P2、P3 全套圖卡 100% 穩定存檔。

### 🚀 v47.3 日夜盤微觀結構速報當下即時盤態單向強防護鎖定 (2026-08-27)
- **當下即時盤態單向鎖定 (Microstructure Express Current Market Focus Lock)**：
  - 徹底鎖定 `📌 日夜盤微觀結構速報` 於最新/當下實時盤態與即時 Tick 價位，避免點擊 5 日歷史表格列時干擾當下解讀。
  - 保留 5 日表格與 Satellite GEX 雲圖的歷史切換功能，但速報卡片永遠專注於最新市場情境 (如即時現價與當前 Zero Gamma / Call Wall / Put Wall 距離)。
| **`v47.1`** | 2026-08-26 | 🏛️ **證交所 BFI82U 現貨三大法人買賣超金額對齊三大券商日報 (含鉅額對敲)** |
| **`v47.0`** | 2026-08-26 | 🧲 **Max Pain 空間籌碼結構拓撲 & 融資維持率 21:00 TWSE 雙軌清算分離** |
| **`v46.2`** | 2026-08-26 | 🏛️ **期交所/證交所全官方 Endpoint 對齊 & 動態解讀卡 & 融資維持率四級燈號** |
| **`v45.6`** | 2026-08-26 | 💡 **融資維持率算式與 Lumi 雙重恐慌洗盤共振警報規格編制** |
| **`v45.5`** | 2026-08-25 | 🌐 **期交所官方外匯引擎 (dailyFXRate)** & 美元指數 (DXY) 交割價精準校準 |
| **`v45.4`** | 2026-08-25 | ⚡ **富邦 Neo API 實時 WebSocket 報價網關** & Zero Gamma 雙圖動態連動 |
| **`v45.3`** | 2026-08-24 | 📸 **社群圖卡動態對齊** & TWSE MIS 現貨加權/櫃買動態價差趴數引擎 |
| **`v45.2`** | 2026-08-24 | ☀️/🌙 **Card 8 日夜盤雙層卡片重構** & VEX/GEX+ 補齊 Payload Key |
| **`v45.0`** | 2026-08-23 | 🌀 **VEX (Vanna 恐慌曝險)** & GEX+ Flip (早鳥警報線) & 1:1 社群圖卡生成器 |
| **`v44.1`** | 2026-08-22 | ☀️/🌙 **即時行情時段路由修正** & 個人自用與公開點位法規合規性釋疑 |
| **`v44.0`** | 2026-08-21 | 🛡️ **單層獨立面板 DOM 重構** & 防碰撞雙軌標籤與社群防偽角落浮水印 |
| **`v43.0`** | 2026-08-20 | 🏛️ **Evan T型報價視角重構** & Call/Put 左右分離、750px 豎軸與半透明版權浮水印 |
| **`v42.0`** | 2026-08-19 | 🎬 **衛星雲圖演變播放器** & 100% 官方 API 去券商化無縫合規架構 |
| **`v1.0~v41.0`** | 早期奠基階段 | 🐣 **量化籌碼基因奠基**：從 5K Strategy Master、動能鳥指標到 TXO 全自動 Python 算式網關 |

---

## 🎯 各版本詳細更新紀錄

### 🚀 v49.4 期交所官方夜盤即時成交量 (marketCode=1 Volume) 直連寫入與夜盤個股期真理源對齊版 (2026-09-01)
- **夜盤真實成交量 (Night Real Volume) 對齊**：
  - 升級 etch_taifex_official_night_stock_futures()，從期交所 utDailyMarketExcel?marketCode=1 官方夜盤端點精準提取 cols[8] 夜盤真實成交量與 cols[6] 漲跌點數。
  - 徹底排除將日盤總成交量誤寫入夜盤之問題，全系統 6 大夜盤股期/ETF成交量對齊期交所官方公告。
- **4-Tier 熔斷防範與 0 價避險保護網**：
  - 強化 etch_and_calc_vision.py 數據生成器，確保期交所清晨 05:00~06:15 結算維護期不落入空值，寫入無零價保證。


### 🚀 v47.1 證交所 BFI82U 現貨三大法人買賣超金額對齊 (2026-08-26)
- **修正 BFI82U 解析覆蓋 Bug**：修復舊版 `if '外資' in name:` 在循環中被 `外資自營商` 覆蓋 `外資及陸資(不含外資自營商)` 之問題，完全涵蓋盤後鉅額對敲交易。
- **三大法人買賣超 100% 零誤差對齊永豐/富邦/台新三大券商日報**：
  - **外資買賣超**：`外資及陸資(不含外資自營商)` (如 `+366.13 億 TWD`)。
  - **投信買賣超**：`投信` (如 `+33.66 億 TWD`)。
  - **自營商買賣超**：`自營商(自行買賣 + 避險)` (如 `+179.34 億 TWD`)。
  - **三大法人合計**：`合計` (如 `+579.13 億 TWD`)。

### 🚀 v47.0 Max Pain 空間籌碼結構拓撲 & 融資維持率 21:00 TWSE 雙軌清算分離 (2026-08-26)
- **Max Pain 空間籌碼結構拓撲 (Spatial Topology Matrix)**：依據 `Max Pain`、`Put Wall` 與 `Call Wall` 相對空間位置動態判定 🔴 型態 A (痛點沉底/下檔磁吸 / `Max Pain < Put Wall`)、🟡 型態 B (對稱健康箱體 / `Put Wall <= Max Pain <= Call Wall`) 與 🟢 型態 C (空頭恐慌避險 / `Put Wall << Max Pain`)，符合台股紅多綠空標準色彩。
- **UI 動態 Badge 與 Modal 內建教學指南**：頂部 MAX PAIN KPI 卡片整合動態 Badge 與 `🎓 拓撲診斷 ℹ️` 按鈕，單擊直達【判讀教學指南 Modal】第 4 區塊拓撲診斷對照表與 2腳/4腳精確下單 SOP。
- **融資維持率 21:00 TWSE 清算時序與夜盤雙軌分離**：
  - **夜盤 Session 嚴謹分離**：個股在夜盤休市無成交價與信用交易變動，標示 `- (非交易時段)` 與 `夜盤休市無數據`。
  - **日盤 Session 21:00 時序清算**：盤後 (如 16:00) 證交所尚未公布當日信用交易時，標示金黃虛線膠囊 `未公布 (21:00更新)`；晚間 21:00 或隔晨 06:00 自動補齊數字與四級燈號 (`155.8% 🟢 安定` / `個股 141.2%`)。

### 🚀 v46.2 期交所/證交所全官方 Endpoint 對齊 & 動態解讀卡 (2026-08-26)
- **期交所 100% 真實成交量個股期貨榜首引擎 (Module 10)**：353 檔契約對接 `stockMargining` 與 `futDailyMarketExcel?commodity_id=STF`，榜首排序與口數 100% 零誤差對齊期交所官方 SSF 熱力圖。
- **本日籌碼體質與選擇權解讀卡動態化 (`executive_digest`)**：清除寫死舊點位 (46,050 / 45,920.5)，由 TAIFEX 當日數據實時演算現價、Zero Gamma、Call/Put Wall (45,400 / 45,000) 與 P/C Ratio (113.2%)。
- **Gemini AI 掃描卡對接除權息 API (`ai_ex_dividend_digest`)**：對接期交所 `contractAdj` 與證交所 `TWT48U/49U`，解析 229 檔最新除權息扣點資訊（如川湖 $51元、世芯-KY $32.55元、台光電 $25元、台積電 $4元）。
- **雙軌融資維持率四級動態燈號**：表 2 矩陣最右欄整合全市場整戶 vs 純個股維持率，並帶入四級動態燈號標籤 (`🟢 安定` / `🟡 常態` / `🟠 警戒` / `🔴 斷頭洗盤`)。
- **社群圖卡 Card 8 區分日夜盤雙視角**：重構 `generate_social_card.py` Card 8 HTML 模板為 `☀️ 日盤 (13:45)` 與 `🌙 夜盤校正` 雙層排版。

### 💡 v45.6 籌碼研討與量化規劃紀錄 (2026-08-26)
- **官方數據源**：臺灣證券交易所 (TWSE) 每日 `信用交易統計 (MI_MARGIN)` API。
- **全市場融資維持率（含 ETF，XQ 視角）**：反映全市場「槓桿籌碼是否徹底沉澱/退場」。
- **個股融資維持率（不含 ETF，玩股網視角）**：扣除 ETF 類別後單獨計算，率先預警中小型飆股與單一個股的追繳斷頭潮。
- **Lumi 雙重共振機制**：當 VEX 恐慌指數觸發 [助跌/Panic] 且大盤融資維持率降至 `< 145%` 警戒區 ➜ 觸發 Telegram 雙重極致洗盤/強烈 V 轉共振警報。

### 🌐 v45.5 期交所官方外匯引擎 & 高精度數據校準 (2026-08-25)
- **期交所官方 API 數據對齊**：全面廢棄傳統國際場外點位（Yahoo FX），改為直接解析台灣期交所官方每日外幣參考匯率（`dailyFXRate`）。
- **美元/台幣與美元/日圓 100% 精準校準**：完全符合央行與期交所每日 16:00 官方公告基準，徹底消除場外即時價位與交割參考價的點位落差。
- **DXY 美元指數期貨高精度交割對齊**：校正 DXY 多日滾動漲跌幅與點位計算。

### ⚡ v45.4 富邦 Neo API 實時行情串流網關 & 動態 Zero Gamma (2026-08-25)
- **Fubon MarketData WebSocket 原生串接**：完成本機行情網關對富邦 Neo API SDK 的全功能實接，實現台指期近月合約 Tick 價位與漲跌幅極速推播。
- **Live Tick 動態跳動與 Zero Gamma 雙圖同步**：即時點位更新觸發紅綠閃爍動畫，Zero Gamma 依 Gamma/Vanna 曲線即時推演最新防守臨界點。

### 📸 v45.3 社群圖卡數據全動態對齊 (2026-08-24)
- **徹底清除硬編碼**：重構 `generate_social_card.py` HTML 模板，全指標動態解構由 Payload 產出的 `gex_data.json`。
- **TWSE MIS 現貨與櫃買指數動態漲跌點數與趴數引擎**：即時算出現貨加權指數與櫃買指數的漲跌金額與漲跌幅。

### ☀️/🌙 v45.2 Card 8 日夜盤雙層卡片重構 (2026-08-24)
- **Card 8 雙層卡片**：同時呈現日盤 (13:45) 與夜盤校正 (05:00) 雙套轉折價位與 VEX 恐慌金額。
- **補齊 Payload Key (`gex_plus_flip`)**：修復 Payload 返回物件遺漏 `gex_plus_flip` 欄位之 Bug。

### 🌀 v45.0 高階 VEX 恐慌曝險 & GEX+ Flip & 1:1 社群圖卡 (2026-08-23)
- **Black-Scholes Vanna 算式**：計算各履約價與全場做市商 Vanna 恐慌曝險。
- **GEX+ Flip (合成轉折點)**：導出結合價格波動與恐慌情緒的靈敏轉折線。
- **1:1 正方形社群圖卡生成器**：自動產生 1080x1080 像素、1:1 正方形黃金比例圖卡，配備 5 大位階多通道防碰撞引擎。

### ☀️/🌙 v44.1 即時行情時段路由修正 (2026-08-22)
- **日夜盤路由修復**：日盤點位正確寫入 `☀️ 日盤` 欄位並顯示漲跌閃爍動畫。
- **法規合規性釋疑**：明確分流「富邦 API (個人自用)」與「期交所/證交所 MIS (公開資訊合理使用)」。

### 🏛️ v43.0 Evan T型報價視角與全站浮水印 (2026-08-20)
- **T型報價視角 (DEFAULT)**：預設 Y 軸為履約價 Strike / X 軸為 GEX 金額，符合台灣期貨選擇權 T 型報價表視覺直覺。
- **左右側 Call/Put 籌碼分離**：Call GEX 壓在右側 (+X)，Put GEX 壓在左側 (-X)，中間貫穿 Net GEX S 曲線。
- **全站半透明浮水印防偽**：面板與卡片烙印 `© 尋鳥 Bluebird Finder` 半透明版權宣告。

### 🎬 v42.0 衛星雲圖演變播放器與去券商化合規架構 (2026-08-19)
- **10 盤籌碼動態播放器**：提供 1.2 秒間隔自動播放過去 5 天 10 個日夜盤 GEX 位移演變。
- **100% TAIFEX/TWSE 公開 API 直連**：建立個人研究合規去券商化資料抓取層，實現擺脫第三方資料限制。

### 🐣 v1.0 ~ v41.0 早期奠基與量化基因演進 (歷史前身)
- ** TradingView Pine Script 指標體系**：
  - `5K Strategy Master v1 ~ v5`（台指 5 分 K 策略主體、保本與動態追蹤停損機制）
  - `動能鳥指標 (Momentum Bird Indicator) v1 ~ v6`（多時框波動度爆發與瀑布告警系統）
  - `DeMark V1 ~ V3`（DeMark 九轉與個股/原物料專武版）
- ** Python 全自動量化數據處理網關**：
  -期交所 (TAIFEX) 大額交易人與三大法人期權買賣超金額算式網關。
  - Cloudflare Worker 雲端中繼站與 Telegram 機器人 24/7 自動發送警報架構。
  - 此階段奠定了 `v42.0` 起本 Web 儀表板成立時強大的籌碼算式底蘊與量化架構！

---

*詳細最新現狀請參閱：[STATUS.md](STATUS.md) | 專案交接 SOP 請參閱：[PROJECT_HANDOVER.md](PROJECT_HANDOVER.md)*
